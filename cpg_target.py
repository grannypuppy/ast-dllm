def foo(x, MAX):
    # This function demonstrates a simple conditional branch
    if x < MAX:
        y = 2 * x
        print(y)

# --- Main execution part ---
# These are the statements that will be analyzed
x = 10
MAX = 20
foo(x, MAX)
